
log "REPORT"
echo "Domain:         example.com"
echo "PTR at your provider: 203.0.113.10 -> relay.example.com (critical for deliverability)."
echo "Backup passphrase (keep it — encrypted backups need it): $(cat /etc/gryonixnexus/autobackup.pass 2>/dev/null)"
echo
echo "Service status:"
for s in nftables docker caddy; do
  printf '  %-14s %s\n' "$s" "$(systemctl is-active "$s" || true)"
done
echo
echo "Remember the DNS records from dns-records.txt — without them Caddy cannot issue certificates."

echo
echo "=== SERVICE CREDENTIALS (save these) ==="
echo "Passbolt:     https://vault.example.com"
if [ -s /opt/passbolt/.registration-url ]; then
  echo "  Finish setting up the administrator (this link works ONCE):"
  echo "  $(cat /opt/passbolt/.registration-url)"
else
  echo "  No setup link available here — if an administrator was already invited, sign in with that account; otherwise check the container logs and invite one manually (cake passbolt register_user)."
fi

